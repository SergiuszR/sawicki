{
  "name": "saw-project",
  "version": "1.0.0",
  "description": "SAW Project Global JavaScript",
  "main": "src/main.js",
  "scripts": {
    "lint": "eslint src/**.js",
    "build": "npm run lint && esbuild src/main.js --bundle --outfile=dist/total.js --minify",
    "dev": "npm run lint && esbuild src/main.js --bundle --outfile=dist/total.js --servedir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "eslint": "^8.0.0"
  },
  "author": "",
  "license": "ISC"
}
